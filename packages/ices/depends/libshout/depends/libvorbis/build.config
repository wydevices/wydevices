# Generic package building template.
#
# Remove not used variables.
#
# * Binaries will be searched on:
#    - Built packages
#    - STLinux binaries
#    - 'scripts' folder
#
# * Files in this folders will be
#   copied to final package:
#    - config-files
#    - static-files
#

# package name
NAME="libvorbis"

# package version
VERSION=1.3.4

# other packages that will be included
SUBPACKAGES=""

# package dependencies
DEPENDS=""

# url to download sources file
URL="http://downloads.xiph.org/releases/vorbis/libvorbis-$VERSION.tar.gz"

# name of sources file
SRCFILE="libvorbis-$VERSION.tar.gz"

# sources dir
SRCDIR="libvorbis-$VERSION"

# actions before compilation
PRE_COMPILATION() {
BASEPATH=`pwd`
}

# configure flags
CONFIGUREFLAGS="--host=sh4-linux \
		--prefix=/wymedia/usr \
		--disable-oggtest \
		--with-ogg-libraries=$BASEPATH/../libogg/libogg-1.3.1/src/.libs \
		--with-ogg-includes=$BASEPATH/../libogg/libogg-1.3.1/include/ "

# actions before building the wybox package
#PRE_WYBOX_PKG_BUILD() {

#}

# binary files to include in final package
BINARIES=""

# actions to clean compilation files
#CLEAN() {

#}

