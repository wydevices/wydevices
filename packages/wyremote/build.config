NAME="wyremote"

VERSION="0.4"

DEPENDS="wymodcp-php"

PRE_WYBOX_PKG_BUILD() {
        mkdir -p $PKGDIR/usr/bin/wyremote
        mkdir -p $PKGDIR/usr/etc/wyremote
        cp -rp $PKGDIR/files/* $PKGDIR/usr/
}
