# Echo client program
import socket, os

s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
s.connect("/wymedia/tmp/wymodcp_socket")
while True:
	data = raw_input('Enter a command:\n')
	s.send(data)
	os.system('clear')
	data = s.recv(1024)
	print 'Result: ', repr(data)
s.close()

